# Vou deixar comentado se não ele vai passar na esteira CI/CD

# name: Deploy to AWS
# 
# on:
#   workflow_run:
#     workflows: ["CI Workflow"] 
#     types:
#       - completed
#     branches: [ "main" ]
# 
# jobs:
#   deploy:
#     # Só executa se o CI terminou com SUCESSO
#     if: ${{ github.event.workflow_run.conclusion == 'success' }}
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
# 
#       - name: Configure AWS credentials
#         uses: aws-actions/configure-aws-credentials@v2
#         with:
#           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws-region: us-east-1
# 
#       - name: Login to Amazon ECR
#         id: login-ecr
#         uses: aws-actions/amazon-ecr-login@v1
# 
#       - name: Build and Push Docker Image
#         env:
#           ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
#           REPOSITORY: grotrack-frontend
#         run: |
#           docker build -t $ECR_REGISTRY/$REPOSITORY:latest .
#           docker push $ECR_REGISTRY/$REPOSITORY:latest
# 
#       - name: SSH Deploy to EC2
#         uses: appleboy/ssh-action@master
#         with:
#           host: ${{ secrets.EC2_HOST }}
#           username: ubuntu
#           key: ${{ secrets.SSH_PRIVATE_KEY }}
#           script: |
#             aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin ${{ steps.login-ecr.outputs.registry }}
#             docker stop grotrack || true
#             docker rm grotrack || true
#             docker pull ${{ steps.login-ecr.outputs.registry }}/grotrack-frontend:latest
#             docker run -d --name grotrack -p 80:80 ${{ steps.login-ecr.outputs.registry }}/grotrack-frontend:latest